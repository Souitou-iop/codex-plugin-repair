name: Check repair scripts

on:
  push:
  pull_request:

jobs:
  bash:
    name: Bash script (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: Linux
          - os: macos-latest
            platform: Darwin
    steps:
      - uses: actions/checkout@v5

      - name: Syntax check
        run: bash -n scripts/fix-codex-plugins.sh

      - name: Verify cancel path
        shell: bash
        run: |
          set -euo pipefail
          cancel_home="$RUNNER_TEMP/codex-home-cancel"
          mkdir -p "$cancel_home"
          printf 'service_tier = "default"\n' > "$cancel_home/config.toml"
          printf '1\nn\n' | CODEX_HOME="$cancel_home" ./scripts/fix-codex-plugins.sh > "$RUNNER_TEMP/cancel-output.txt"
          grep -q '已取消，未做任何修改。' "$RUNNER_TEMP/cancel-output.txt"
          if [[ "${{ matrix.platform }}" == "Linux" ]]; then
            grep -q '在 Linux 上不启用 Desktop 专属 bundled 插件' "$RUNNER_TEMP/cancel-output.txt"
          fi
          if [[ "${{ matrix.platform }}" == "Darwin" ]]; then
            grep -q '在 macOS 上启用 Browser、Chrome、Computer Use bundled 插件' "$RUNNER_TEMP/cancel-output.txt"
          fi
          grep -q 'service_tier = "default"' "$cancel_home/config.toml"
          if compgen -G "$cancel_home/config.toml.bak-plugin-repair-*" > /dev/null; then
            exit 1
          fi

      - name: Build mock Codex home
        shell: bash
        run: |
          set -euo pipefail
          CODEX_HOME="$RUNNER_TEMP/codex-home"
          mkdir -p "$CODEX_HOME"
          cat > "$CODEX_HOME/config.toml" <<'EOF'
          service_tier = "default"

          [plugins."codex-security@openai-curated"]
          enabled = true

          [plugins."mystery@openai-unknown"]
          enabled = true
          EOF

          for plugin in browser chrome computer-use; do
            root="$CODEX_HOME/.tmp/bundled-marketplaces/openai-bundled/plugins/$plugin"
            mkdir -p "$root/.codex-plugin"
            printf '{"name":"%s","version":"1.0.0"}\n' "$plugin" > "$root/.codex-plugin/plugin.json"
          done

          mkdir -p "$CODEX_HOME/.tmp/plugins/.agents/plugins"
          printf '{}\n' > "$CODEX_HOME/.tmp/plugins/.agents/plugins/marketplace.json"
          root="$CODEX_HOME/.tmp/plugins/plugins/codex-security"
          mkdir -p "$root/.codex-plugin"
          printf '{"name":"codex-security","version":"2.0.0"}\n' > "$root/.codex-plugin/plugin.json"

          root="$CODEX_HOME/plugins/cache/openai-unknown/mystery/9.9.9"
          mkdir -p "$root/.codex-plugin"
          printf '{"name":"mystery","version":"9.9.9"}\n' > "$root/.codex-plugin/plugin.json"

          stale="$CODEX_HOME/plugins/cache/openai-bundled/browser/1.0.0"
          mkdir -p "$stale"
          printf 'incomplete\n' > "$stale/README.txt"

          echo "CODEX_HOME=$CODEX_HOME" >> "$GITHUB_ENV"

      - name: Run repair script
        env:
          CODEX_PLUGIN_REPAIR_PLATFORM: ${{ matrix.platform }}
          CODEX_PLUGIN_REPAIR_TIMESTAMP: "2026-06-04T00:00:00Z"
          CODEX_PLUGIN_REPAIR_LANGUAGE: zh-CN
          CODEX_PLUGIN_REPAIR_YES: "1"
        run: |
          set -euo pipefail
          ./scripts/fix-codex-plugins.sh 2>&1 | tee "$RUNNER_TEMP/repair-output.txt"

      - name: Verify repaired state
        shell: bash
        run: |
          set -euo pipefail
          test -f "$CODEX_HOME/config.toml"
          grep -q 'service_tier = "fast"' "$CODEX_HOME/config.toml"
          grep -q '\[1/5\] 检查 Codex 配置...' "$RUNNER_TEMP/repair-output.txt"
          grep -q '插件覆盖报告 / Plugin coverage report:' "$RUNNER_TEMP/repair-output.txt"
          grep -q '修复完成。' "$RUNNER_TEMP/repair-output.txt"
          grep -q '后续建议：' "$RUNNER_TEMP/repair-output.txt"
          grep -q 'OK mystery@openai-unknown marketplace=false source=false cache=true' "$RUNNER_TEMP/repair-output.txt"
          test -d "$CODEX_HOME/plugins/cache/openai-curated/codex-security/2.0.0"
          test -d "$CODEX_HOME/plugins/cache/openai-unknown/mystery/9.9.9"
          if [[ "${{ matrix.platform }}" == "Darwin" ]]; then
            for plugin in browser chrome computer-use; do
              grep -q "\[plugins.\"$plugin@openai-bundled\"\]" "$CODEX_HOME/config.toml"
              test -d "$CODEX_HOME/plugins/cache/openai-bundled/$plugin/1.0.0"
              test -L "$CODEX_HOME/plugins/cache/openai-bundled/$plugin/latest"
            done
            test -f "$CODEX_HOME/plugins/cache/openai-bundled/browser/1.0.0/.codex-plugin/plugin.json"
            compgen -G "$CODEX_HOME/plugins/cache/openai-bundled/browser/1.0.0.bak-plugin-repair-*" > /dev/null
          fi

      - name: Verify diagnostic log on failure
        shell: bash
        run: |
          set -euo pipefail
          fail_home="$RUNNER_TEMP/codex-home-fail"
          mkdir -p "$fail_home/plugins/cache/openai-unknown/empty-plugin"
          cat > "$fail_home/config.toml" <<'EOF'
          [plugins."empty-plugin@openai-unknown"]
          enabled = true
          EOF
          if CODEX_HOME="$fail_home" CODEX_PLUGIN_REPAIR_PLATFORM="Linux" CODEX_PLUGIN_REPAIR_LANGUAGE="zh-CN" CODEX_PLUGIN_REPAIR_YES="1" ./scripts/fix-codex-plugins.sh > "$RUNNER_TEMP/failure-output.txt" 2>&1; then
            cat "$RUNNER_TEMP/failure-output.txt"
            exit 1
          fi
          grep -q '仍有插件缺失 / Still missing: empty-plugin@openai-unknown' "$RUNNER_TEMP/failure-output.txt"
          grep -q '诊断日志 / Diagnostic log:' "$RUNNER_TEMP/failure-output.txt"
          grep -q 'Agents / Codex' "$RUNNER_TEMP/failure-output.txt"
          log_path=$(find "$fail_home" -maxdepth 1 -name 'codex-plugin-repair-diagnostics-*.log' | head -n 1)
          test -n "$log_path"
          grep -q 'missing=empty-plugin@openai-unknown' "$log_path"
          grep -q 'MISSING empty-plugin@openai-unknown marketplace=false source=false cache=false' "$log_path"

  powershell:
    name: PowerShell script (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Parse PowerShell script
        shell: pwsh
        run: |
          $tokens = $null
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path scripts/Fix-CodexPlugins.ps1), [ref]$tokens, [ref]$errors) | Out-Null
          if ($errors.Count -gt 0) {
            $errors | Format-List *
            exit 1
          }

      - name: Verify cancel path
        shell: pwsh
        run: |
          $cancelHome = Join-Path $env:RUNNER_TEMP "codex-home-cancel"
          New-Item -ItemType Directory -Path $cancelHome -Force | Out-Null
          'service_tier = "default"' | Set-Content -LiteralPath (Join-Path $cancelHome "config.toml") -NoNewline
          $outputPath = Join-Path $env:RUNNER_TEMP "cancel-output.txt"
          $env:CODEX_PLUGIN_REPAIR_PLATFORM = "Windows"
          "n" | pwsh -NoProfile -File ./scripts/Fix-CodexPlugins.ps1 -CodexHome $cancelHome -Language zh-CN > $outputPath 2>&1
          if ($LASTEXITCODE -ne 0) {
            Get-Content -LiteralPath $outputPath
            exit 1
          }
          $output = Get-Content -LiteralPath $outputPath -Raw
          if ($output -notmatch '在 Windows 上启用 Browser、Chrome、Computer Use bundled 插件') { exit 1 }
          if ($output -notmatch '已取消，未做任何修改。') { exit 1 }
          $config = Get-Content -LiteralPath (Join-Path $cancelHome "config.toml") -Raw
          if ($config -notmatch 'service_tier = "default"') { exit 1 }
          $backups = @(Get-ChildItem -LiteralPath $cancelHome -Filter 'config.toml.bak-plugin-repair-*')
          if ($backups.Count -ne 0) { exit 1 }

      - name: Build mock Codex home
        shell: pwsh
        run: |
          $CodexHome = Join-Path $env:RUNNER_TEMP "codex-home"
          New-Item -ItemType Directory -Path $CodexHome -Force | Out-Null
          @'
          service_tier = "default"

          [plugins."codex-security@openai-curated"]
          enabled = true

          [plugins."mystery@openai-unknown"]
          enabled = true
          '@ | Set-Content -LiteralPath (Join-Path $CodexHome "config.toml") -NoNewline

          foreach ($plugin in @("browser", "chrome", "computer-use")) {
            $root = Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/$plugin"
            New-Item -ItemType Directory -Path (Join-Path $root ".codex-plugin") -Force | Out-Null
            "{`"name`":`"$plugin`",`"version`":`"1.0.0`"}" | Set-Content -LiteralPath (Join-Path $root ".codex-plugin/plugin.json") -NoNewline
          }

          $existingBundled = Join-Path $CodexHome ".tmp/bundled-marketplaces/openai-bundled/plugins/chrome/.codex-plugin"
          New-Item -ItemType Directory -Path $existingBundled -Force | Out-Null
          '{"name":"chrome","version":"0.0.1"}' | Set-Content -LiteralPath (Join-Path $existingBundled "plugin.json") -NoNewline

          New-Item -ItemType Directory -Path (Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/browser/scripts") -Force | Out-Null
          "" | Set-Content -LiteralPath (Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/browser/scripts/browser-client.mjs") -NoNewline
          New-Item -ItemType Directory -Path (Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/chrome/scripts") -Force | Out-Null
          New-Item -ItemType Directory -Path (Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/chrome/extension-host/windows/x64") -Force | Out-Null
          "" | Set-Content -LiteralPath (Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/chrome/scripts/browser-client.mjs") -NoNewline
          "" | Set-Content -LiteralPath (Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/chrome/extension-host/windows/x64/extension-host.exe") -NoNewline
          New-Item -ItemType Directory -Path (Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/computer-use/scripts") -Force | Out-Null
          New-Item -ItemType Directory -Path (Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/computer-use/node_modules/@oai/sky/bin/windows") -Force | Out-Null
          "" | Set-Content -LiteralPath (Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/computer-use/scripts/computer-use-client.mjs") -NoNewline
          "" | Set-Content -LiteralPath (Join-Path $env:RUNNER_TEMP "appx-bundled/plugins/computer-use/node_modules/@oai/sky/bin/windows/codex-computer-use.exe") -NoNewline

          $staleCache = Join-Path $CodexHome "plugins/cache/openai-bundled/computer-use/1.0.0/.codex-plugin"
          New-Item -ItemType Directory -Path $staleCache -Force | Out-Null
          '{"name":"computer-use","version":"1.0.0"}' | Set-Content -LiteralPath (Join-Path $staleCache "plugin.json") -NoNewline

          New-Item -ItemType Directory -Path (Join-Path $CodexHome ".tmp/plugins/.agents/plugins") -Force | Out-Null
          "{}" | Set-Content -LiteralPath (Join-Path $CodexHome ".tmp/plugins/.agents/plugins/marketplace.json") -NoNewline
          $root = Join-Path $CodexHome ".tmp/plugins/plugins/codex-security"
          New-Item -ItemType Directory -Path (Join-Path $root ".codex-plugin") -Force | Out-Null
          '{"name":"codex-security","version":"2.0.0"}' | Set-Content -LiteralPath (Join-Path $root ".codex-plugin/plugin.json") -NoNewline

          $root = Join-Path $CodexHome "plugins/cache/openai-unknown/mystery/9.9.9"
          New-Item -ItemType Directory -Path (Join-Path $root ".codex-plugin") -Force | Out-Null
          '{"name":"mystery","version":"9.9.9"}' | Set-Content -LiteralPath (Join-Path $root ".codex-plugin/plugin.json") -NoNewline

          "CODEX_HOME=$CodexHome" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Run repair script
        shell: pwsh
        env:
          CODEX_PLUGIN_REPAIR_PLATFORM: Windows
          CODEX_PLUGIN_REPAIR_BUNDLED_SOURCE_ROOT: ${{ runner.temp }}/appx-bundled
          CODEX_PLUGIN_REPAIR_TIMESTAMP: "2026-06-04T00:00:00Z"
          CODEX_PLUGIN_REPAIR_LANGUAGE: zh-CN
          CODEX_PLUGIN_REPAIR_YES: "1"
        run: |
          $outputPath = Join-Path $env:RUNNER_TEMP "repair-output.txt"
          Start-Transcript -Path $outputPath -Force | Out-Null
          try {
            ./scripts/Fix-CodexPlugins.ps1
          } finally {
            Stop-Transcript | Out-Null
          }

      - name: Verify repaired state
        shell: pwsh
        run: |
          $config = Get-Content -LiteralPath (Join-Path $env:CODEX_HOME "config.toml") -Raw
          $output = Get-Content -LiteralPath (Join-Path $env:RUNNER_TEMP "repair-output.txt") -Raw
          if ($config -notmatch 'service_tier = "fast"') { exit 1 }
          if ($output -notmatch '\[1/5\] 检查 Codex 配置...') { exit 1 }
          if ($output -notmatch '插件覆盖报告 / Plugin coverage report:') { exit 1 }
          if ($output -notmatch '修复完成。') { exit 1 }
          if ($output -notmatch '后续建议：') { exit 1 }
          if ($output -notmatch 'OK mystery@openai-unknown marketplace=false source=false cache=true') { exit 1 }
          foreach ($plugin in @("browser", "chrome", "computer-use")) {
            if ($config -notmatch "\[plugins\.\`"$plugin@openai-bundled\`"\]") { exit 1 }
            $cache = Join-Path $env:CODEX_HOME "plugins/cache/openai-bundled/$plugin/1.0.0"
            if (-not (Test-Path -LiteralPath $cache -PathType Container)) { exit 1 }
            $latest = Join-Path $env:CODEX_HOME "plugins/cache/openai-bundled/$plugin/latest"
            if (-not (Test-Path -LiteralPath $latest)) { exit 1 }
            $latestItem = Get-Item -LiteralPath $latest
            if (-not ($latestItem.Attributes -band [System.IO.FileAttributes]::ReparsePoint)) { exit 1 }
          }
          $helper = Join-Path $env:CODEX_HOME "plugins/cache/openai-bundled/computer-use/1.0.0/node_modules/@oai/sky/bin/windows/codex-computer-use.exe"
          if (-not (Test-Path -LiteralPath $helper -PathType Leaf)) { exit 1 }
          $escapedHelper = [regex]::Escape($helper)
          if ($config -notmatch $escapedHelper) { exit 1 }
          $backupCaches = @(Get-ChildItem -LiteralPath (Join-Path $env:CODEX_HOME "plugins/cache/openai-bundled/computer-use") -Directory -Filter "*.bak-plugin-repair-*")
          if ($backupCaches.Count -lt 1) { exit 1 }
          $bundledBackups = @(Get-ChildItem -LiteralPath (Join-Path $env:CODEX_HOME ".tmp/bundled-marketplaces") -Directory -Filter "openai-bundled.bak-plugin-repair-*")
          if ($bundledBackups.Count -lt 1) { exit 1 }
          if ($config -match "bak-plugin-repair") { exit 1 }
          $curated = Join-Path $env:CODEX_HOME "plugins/cache/openai-curated/codex-security/2.0.0"
          if (-not (Test-Path -LiteralPath $curated -PathType Container)) { exit 1 }
          $unknown = Join-Path $env:CODEX_HOME "plugins/cache/openai-unknown/mystery/9.9.9"
          if (-not (Test-Path -LiteralPath $unknown -PathType Container)) { exit 1 }

      - name: Verify diagnostic log on failure
        shell: pwsh
        run: |
          $failHome = Join-Path $env:RUNNER_TEMP "codex-home-fail"
          New-Item -ItemType Directory -Path (Join-Path $failHome "plugins/cache/openai-unknown/empty-plugin") -Force | Out-Null
          @'
          [plugins."empty-plugin@openai-unknown"]
          enabled = true
          '@ | Set-Content -LiteralPath (Join-Path $failHome "config.toml") -NoNewline
          $outputPath = Join-Path $env:RUNNER_TEMP "failure-output.txt"
          $env:CODEX_HOME = $failHome
          $env:CODEX_PLUGIN_REPAIR_PLATFORM = "Linux"
          $env:CODEX_PLUGIN_REPAIR_LANGUAGE = "zh-CN"
          $env:CODEX_PLUGIN_REPAIR_YES = "1"
          pwsh -NoProfile -File ./scripts/Fix-CodexPlugins.ps1 > $outputPath 2>&1
          if ($LASTEXITCODE -eq 0) {
            Get-Content -LiteralPath $outputPath
            exit 1
          }
          $output = Get-Content -LiteralPath $outputPath -Raw
          Get-Content -LiteralPath $outputPath
          if ($output -notmatch 'Still missing: empty-plugin@openai-unknown') { exit 1 }
          if ($output -notmatch 'Diagnostic log:') { exit 1 }
          if ($output -notmatch 'Agents / Codex') { exit 1 }
          $logs = @(Get-ChildItem -LiteralPath $failHome -Filter 'codex-plugin-repair-diagnostics-*.log')
          if ($logs.Count -ne 1) { exit 1 }
          $log = Get-Content -LiteralPath $logs[0].FullName -Raw
          Get-Content -LiteralPath $logs[0].FullName
          if ($log -notmatch 'missing=empty-plugin@openai-unknown') { exit 1 }
          if ($log -notmatch 'MISSING empty-plugin@openai-unknown marketplace=false source=false cache=false') { exit 1 }
          exit 0
